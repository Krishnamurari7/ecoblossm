import { metadata as sustainabilityMetadata } from "./metadata";

export const metadata = sustainabilityMetadata;

export default function SustainabilityLayout({ children }) {
  return children;
}






